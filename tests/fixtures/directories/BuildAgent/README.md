# Build Agent Fixture

This maintained fixture represents a containerized .NET/NUKE build agent with an
authored PowerShell module contract.

```text
docker run --rm ghcr.io/example/build-agent-fixture:latest
```
